/**
 * Wire protocol — re-exported from the generated mirror.
 *
 * The real types live in `protocol.gen.ts`, which `codegen/generate.mjs` emits
 * verbatim from the single source of truth at `server/src/protocol.ts`.
 * Do NOT hand-edit the protocol here — change the server source and run
 * `cd codegen && npm run gen` (which also regenerates the Flutter Dart models).
 */
export * from './protocol.gen';
